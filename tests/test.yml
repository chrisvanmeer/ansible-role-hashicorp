---
- name: Test
  hosts: localhost
  become: true

  vars:
    hashicorp_products:
      - name: terraform
        version: "1.15.8"
      - name: vault
        version: "2.0.4"
      - name: consul
        version: "2.0.3"

  roles:
    - role: chrisvanmeer.hashicorp
